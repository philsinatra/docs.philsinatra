import { Layout } from '../components/'

export const meta = {
  title: 'Hello World',
  date: '2020-12-22',
}

The landing page for my devleopment notes blog that I don't have time to make interesting.

Built with [Nextjs](https://nextjs.org) using [MDX](https://mdxjs.com) with [Typescript](https://www.typescriptlang.org).

export default ({ children }) => <Layout meta={meta}>{children}</Layout>
